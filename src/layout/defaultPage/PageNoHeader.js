import React from 'react';

const PageNoHeader = ({children}) => {
    return (
        <div className="mt-[60px] h-full w-[1400px] mx-auto">
            {children}
        </div>
    );
};

export default PageNoHeader;